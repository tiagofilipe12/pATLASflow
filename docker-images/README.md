# Docker Images for pATLAS auxiliary scripts

There is a file for each docker image that allow to run:

* mash screen
* mash dist for assemblies  (or sequences) (still not implemented)
* mapping (still not implemented)